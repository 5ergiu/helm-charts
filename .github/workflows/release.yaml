name: 🚀 Release Charts

on:
  push:
    branches:
      - main
    paths:
      - 'charts/**'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (skip actual release)'
        required: false
        type: boolean
        default: false
      chart:
        description: 'Specific chart to release (optional, e.g. "charts/my-chart")'
        required: false
        type: string

concurrency:
  group: release
  cancel-in-progress: true

permissions:
  contents: write
  packages: write

jobs:
  test-charts:
    name: 🧪 Test Charts
    # Skip if triggered by github-actions bot
    if: github.actor != 'github-actions[bot]'
    uses: ./.github/workflows/test-charts.yaml

  build:
    name: 🔨 Build Chart Artifacts
    needs: test-charts
    runs-on: ubuntu-latest
    if: needs.test-charts.outputs.charts != '[]'
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🔧 Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 💿 Install git-cliff
        uses: taiki-e/install-action@git-cliff

      - name: ⎈ Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4.3.1
        with:
          version: v4.0.1

      - name: 🔌 Install helm-schema plugin
        run: |
          if ! helm plugin list | grep -q "schema"; then
            helm plugin install https://github.com/losisin/helm-values-schema-json.git --version v1.9.2
          fi

      - name: 🛠️ Generate artifacts for all changed charts
        run: |
          charts='${{ needs.test-charts.outputs.charts }}'
          echo "Processing charts: $charts"

          # Convert JSON array to bash array
          chart_list=$(echo "$charts" | jq -r '.[]')

          for chart in $chart_list; do
            echo "=========================================="
            echo "Processing chart: $chart"
            echo "=========================================="

            chart_name=$(basename "$chart")

            # Generate schema
            echo "⚙️ Generating schema for $chart_name..."
            helm schema -input "$chart/values.yaml" -output "$chart/values.schema.json" -draft 7

            # Generate changelog
            echo "📝 Generating changelog for $chart_name..."
            git-cliff --verbose --bump --config cliff.toml --output "$chart/CHANGELOG.md" --include-path "$chart/*"

            # Generate ArtifactHub annotations
            echo "📝 Generating ArtifactHub annotations for $chart_name..."
            git-cliff --config cliff-artifacthub.toml --output changes.json --include-path "$chart/*"
            yq eval '.annotations."artifacthub.io/changes" += load("changes.json")' -i "$chart/Chart.yaml"
            rm -f changes.json

            # Bump chart version
            echo "🔼 Bumping chart version for $chart_name..."
            bumped=$(git cliff --bumped-version)
            new_version=$(echo "$bumped" | cut -d'-' -f2-)
            yq -i ".version = \"$new_version\"" "$chart/Chart.yaml"
            echo "✅ New version: $new_version"
          done

      - name: 💾 Commit and push changes
        run: |
          git add charts/*/Chart.yaml charts/*/CHANGELOG.md charts/*/values.schema.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update chart artifacts"
            git push origin main
            echo "✅ Artifacts committed and pushed to main"
          fi

  release:
    name: 🚀 Release Charts
    runs-on: ubuntu-latest
    needs:
      - test-charts
      - build
    if: github.event.inputs.dry_run != 'true'
    outputs:
      changed_charts: ${{ steps.chart-releaser.outputs.changed_charts }}
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          fetch-depth: 0
          ref: main

      - name: 📦 Update chart dependencies
        run: |
          set -euo pipefail
          echo "Updating dependencies for all charts..."
          for chart_dir in charts/*; do
            if [ -f "$chart_dir/Chart.yaml" ]; then
              echo "Processing $chart_dir..."
              if grep -q "^dependencies:" "$chart_dir/Chart.yaml"; then
                echo "  → Updating dependencies for $chart_dir"
                helm dependency update "$chart_dir"
              else
                echo "  → No dependencies found, skipping"
              fi
            fi
          done

      - name: 🚀 Run chart-releaser
        id: chart-releaser
        uses: helm/chart-releaser-action@cae68fefc6b5f367a0275617c9f83181ba54714f # v1.7.0
        with:
          skip_existing: true
        env:
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"

  upload-to-oci:
    name: 📤 Upload to OCI Registry
    needs:
      - test-charts
      - release
    if: needs.release.outputs.changed_charts != ''
    strategy:
      matrix:
        chart: ${{ fromJSON(needs.test-charts.outputs.charts) }}
    uses: ./.github/workflows/oci-upload.yaml
    with:
      chart: ${{ matrix.chart }}
    secrets:
      COSIGN_KEY: ${{ secrets.COSIGN_KEY }}
      COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
      REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}
